// This script is for the side panel's functionality.
// Since we are using an iframe to load the main Next.js app,
// there's no complex JS needed here at the moment.
// If we were building the UI directly in the extension, this is where
// the React app would be mounted.

console.log("Side Panel Loaded");
